import type { RecommLetterType as RecommLetterTypeSchema } from '@infrastructure/database/schema/recomm-letter-types.schema';

/**
 * Recommendation Letter Type Entity
 * 
 * Domain entity representing a recommendation letter type configuration
 */
export type RecommLetterTypeEntity = RecommLetterTypeSchema;

